# Frontend (React + Vite)

This is the UI for the IntelliCompress visualization platform.

## Setup

```bat
cd frontend
npm install
npm run dev
```

Frontend runs on the Vite dev server URL shown in the terminal (commonly http://localhost:5173).

## Backend dependency

The frontend expects the Flask backend at:
- http://localhost:5000

(So start the backend first.)

